# Project validation baseline

## Aggregate production coverage (2026-09-14)

Run `npm run coverage:project` from the repository root after installing the pinned dependencies and hydrating the generated model catalog with `npm run hydrate:model-data`. The command builds offline, runs package tests with a temporary home and blocked external network, and measures the owned `src/` tree of each production workspace: chord, telemetry, ai, agent, protocol, client, server, coding-agent, tui, and session-backends/sqlite-node. Vitest uses V8 coverage for nine packages; c8 measures the TUI Node test suite. The runner combines covered and total counts across files, rejects duplicate ownership, missing or failed packages, and zero-denominator metrics, then requires at least 80% separately for lines, statements, functions, and branches.

| Metric | Covered / total | Baseline | Gate |
| --- | ---: | ---: | --- |
| Lines | 48,294 / 63,136 | 76.49% | Fail |
| Statements | 50,483 / 67,412 | 74.89% | Fail |
| Functions | 7,217 / 10,015 | 72.06% | Fail |
| Branches | 24,085 / 36,214 | 66.51% | Fail |

All ten package test suites completed successfully on this run. The command exited nonzero because the four aggregate metrics are below the required threshold. Application work must raise all four before final validation can pass.

Generated `dist/` output and model catalogs, tests and fixtures, examples, evaluations, scripts, and configuration files are excluded because they are not owned production source. Source files in `src/` are included even when untested. The AI routine suite excludes credential-backed provider tests, OAuth integration tests, live stream/token tests, and end-to-end provider tests; those require explicit live-test validation. The offline OAuth test verifies that a preexisting credential file is not read. Local loopback remains available for server integration tests; external Node sockets and fetch calls are blocked in runner child processes. This guard is not an operating-system network sandbox, so live tests remain excluded explicitly.

## Pi terminal visual capture (2026-09-14)

`npx vitest --run packages/coding-agent/test/pi-terminal-baseline.test.ts` passed six read-card request/error/visual-retry cases at 40, 80, and 120 columns in dark and light themes. The frozen fixture at `packages/coding-agent/test/__snapshots__/pi-terminal-baseline.test.ts.snap` records ANSI writes, the 24-row viewport, and cursor position generated from the Pi renderer at `0238c1144`; later runs compare against that fixture. The retry is a new successful card following an error with synthetic results, not a provider-backed recovery trace. A 1-column attempt failed before capture with `Rendered line 2 exceeds terminal width (2 > 1)`; the component-only 1-column snapshots in `pi-visual-baseline.test.ts.snap` still pass. This is partial T011 evidence, not a completed visual comparison for all workflows.

`npx vitest --run packages/coding-agent/test/pi-trust-terminal-baseline.test.ts` passed six trust-selector focus cases at 40, 80, and 120 columns in dark and light themes. The frozen `pi-trust-terminal-baseline.test.ts.snap` records ANSI writes, viewport, and cursor for initial Trust focus and Do not trust focus in a synthetic `/project`. No trust choice is persisted by this capture.

## Structured-output malformed-input preflight (2026-09-14)

The observed SUR-LIMIT-002 malformed input `--json-schema '{'` was first a failing parser test, then passed in `packages/coding-agent/test/args.test.ts` and the isolated source CLI process test `packages/coding-agent/test/json-schema-cli.test.ts`: exit 1, empty stdout, and exact stderr `Error: --json-schema is not valid JSON: JSON Parse error: Expected '}'`. The process used a temporary home and agent directory and did not dispatch a model request. Syntactically valid schema input exits 1 with `Structured output is not available yet` to avoid silently ignoring a requested contract; that behavior is an explicit temporary gap, not a reference match. The equals form is also routed through preflight to avoid extension-flag fallthrough, but its reference behavior is unobserved. Focused tests passed (88 cases), `npm run check` passed, and `npm run build` passed after network access was granted for the model catalog. The aggregate coverage gate remains below 80%, and valid structured-output scenarios remain open.

## Background/print conflict preflight (2026-09-14)

CLI-246 was first a failing parser test and then passed in `packages/coding-agent/test/args.test.ts` and the isolated source CLI process test `packages/coding-agent/test/background-cli.test.ts`. With `--bg -p noop`, nox exits 1, writes no stdout, explains that print mode cannot start an attachable background session, and creates no configured session directory. Its stderr differs from the reference because the reference advises an available background workflow that nox has not implemented; this is open as [DISC-001](discrepancies.md). A standalone `--bg noop` exits 1 with an explicit unavailable message and no session directory rather than silently dispatching; successful background launch remains unimplemented and unverified. The process used a temporary home and agent/session directories, and no model request was made.

## Synthetic loopback Read observation (2026-09-14)

`python3 -m unittest discover -s specs/001-add-feature-parity/reference/harness -p 'test_*.py'` passed six protocol, rejection, and redaction checks. `python3 specs/001-add-feature-parity/reference/harness/loopback_probe.py <installed-cli>` then completed under a temporary home with a fixed synthetic key and a local-only message endpoint. The [redacted trace](observations/read-loopback-2026-09-14.json) records one auxiliary request, one Read tool-use request, a subsequent text tool result (`1\talpha\n2\tbeta\n3\t`), a final synthetic completion, exit 0, empty stderr, and an unchanged fixture file. The harness stores no system prompts or raw request bodies. This verifies local tool dispatch and formatting under a mock provider; it does not verify real-service model selection, exact missing-file diagnostics, permission decisions, or content limits. T054 and US1-READ-001 remain open.

## Synthetic loopback missing Read observation (2026-09-14)

The harness test for missing-path error shape failed before implementation because `is_missing_trace` did not exist. After the minimal harness change, all seven protocol tests passed. `python3 specs/001-add-feature-parity/reference/harness/loopback_probe.py <installed-cli> missing` exited 0 under a temporary home with a synthetic key and local endpoint. The [redacted trace](observations/read-missing-loopback-2026-09-14.json) records a `Read` call on an absent file, one tool result with `is_error: true`, final synthetic completion, and an unchanged fixture. The harness deliberately redacts the error text, so this establishes only the error flag and continuation shape; exact diagnostics, permission behavior, and real-service semantics remain open.

## Synthetic loopback outside-workspace Read observation (2026-09-14)

The outside-workspace trace test failed before the harness mode was added. With the mode and redacted diagnostic classification in place, all nine harness tests passed. `python3 specs/001-add-feature-parity/reference/harness/loopback_probe.py <installed-cli> outside` used a temporary workspace and a sibling synthetic file, with no `--allowedTools` rule. The [redacted trace](observations/read-outside-loopback-2026-09-14.json) records a `Read` tool result with `is_error: true` and `error_kind: access_denied`; both files remained unchanged and the final synthetic completion exited 0. This identifies a local CLI permission boundary but does not capture the interactive prompt, exact error wording, or real-service behavior. T054 remains open.

## CLI typo dispatch observation (2026-09-14)

In a temporary home, installed CLI 2.1.270 invoked with `udpate` exited 1 with empty stdout and a stderr suggestion for `update`; no interactive session or prompt was observed. The CLI created `.claude.json` and `backups/` in that home before exiting. The [isolated trace](observations/cli-typo-2026-09-14.txt) records only the diagnostic and entry names.

The nox source CLI process test now passes for `udpate`: exit 1, empty stdout, the full observed three-part stderr after executable-name normalization, no API-key diagnostic, and no session directory. It runs with an empty temporary home, isolated agent/session directories, and `PI_OFFLINE=1`; the home remains empty. An ordinary unmatched prompt still reaches the preexisting auth path. The tightened assertion first failed against the short message, then the focused two-case test, 94 nearby CLI cases, and `npm run check` passed at `5570e76f5`. The reference's `.claude.json`/`backups` side effects still differ as [DISC-002](discrepancies.md), so CLI-519 remains open.

## Leading-flag daemon dispatch observation (2026-09-14)

In three isolated temporary homes, installed CLI 2.1.270 routed `--dangerously-skip-permissions daemon status` and `--allow-dangerously-skip-permissions daemon status` to daemon status, reporting an absent supervisor and exiting 1. With `--bare daemon status`, it entered interactive startup and stopped at the login check instead of printing daemon status. The [normalized trace](observations/cli-daemon-leading-flags-2026-09-14.txt) excludes socket paths and credentials. No model request was made. These observations cover CLI-520–522 routing only; they do not establish nox parity.

## Self-hosted environment parser matrix (2026-09-14)

Nine documented `--environment` combinations were probed in separate logged-out temporary homes with a synthetic environment ID. The [redacted matrix](observations/cli-environment-preflight-2026-09-14.json) records exit 1 and empty stdout for each. CLI-525–532 reject their conflicting input before login with distinct stderr diagnostics. CLI-533, bare `--cloud`, reaches the login gate, consistent with treating the bare flag as absent for conflict checks. No provider credentials were available or model request made. This does not verify authenticated dispatch, environment eligibility, or the resulting session.

## Synthetic loopback prompt composition (2026-09-14)

The `prompt_probe.py` harness used a disposable home, a synthetic key, and a localhost-only Messages endpoint. Its two sentinel-summary tests and the nine existing Read harness tests passed. The [redacted trace](observations/prompt-compose-loopback-2026-09-14.json) records one request whose system field contains the replacement marker before the append marker, then synthetic completion with exit 0 and empty stderr. No raw system prompt or request body was retained. This observes CLI-557 ordering under a mock provider; no nox request comparison or real-service claim has been made.

The file-backed mode repeated the observation with two synthetic prompt files. All 12 harness tests passed. The [redacted file trace](observations/prompt-files-loopback-2026-09-14.json) records one request with the file markers in replacement-then-append order, synthetic completion, exit 0, empty stderr, and unchanged fixture files. CLI-558 still needs a matched nox request comparison and real-service verification.

Nox now parses the two file flags, rejects missing files before model work with the observed diagnostics, and sends the selected paths through its existing resource loader. The focused parser, process, and resource-loader suites passed 139 tests from the package working directory; `npm run check` passed. An additional offline composition test passes both synthetic files through the parser, loader, and prompt builder and finds replacement before append. This confirms local assembly, not a wire-level nox request or real-service result. The isolated missing-file process comparison matches exit, stdout, and stderr, while startup home entries differ as [DISC-003](discrepancies.md). CLI-076, CLI-069, and CLI-558 remain open for their other scenarios.

The inline replacement plus a missing replacement file also matches the reference's conflict-before-file-validation decision, exit 1, empty stdout, and exact stderr. The focused nox process suite passed four cases. The reference created `.claude` and `.claude.json`; nox left its disposable home empty, so CLI-261 joins [DISC-003](discrepancies.md). No model call was made.

For CLI-559 and CLI-560, the first process tests failed because nox printed its ordinary `Error: ... requires a value` diagnostic. A targeted entrypoint preflight now prints the reference's lowercase `error: option '<flag> <file>' argument missing` before settings/session work. Both variants match exit 1, empty stdout, exact stderr, and an empty temporary home. The focused prompt-file, typo, and argument suites passed 99 tests; `npm run check` passed. Valid file handling and the other prompt-file leaves remain separately tracked.

For CLI-561 and CLI-562, directory inputs first failed because nox reported a generic non-file condition. The parser now attempts to read each explicit file, preserving the observed missing-file diagnostic while reporting the same host-OS EISDIR read error for a directory. The focused prompt-file, argument, and resource-loader suites passed 140 tests; `npm run check` passed. Exit 1, empty stdout, and stderr match in disposable homes. Nox creates only the supplied directory fixture, while the reference also creates `.claude` and `.claude.json`; this remains [DISC-003](discrepancies.md).

The Read loopback fixture also captured the installed CLI's bare-mode tool catalog without retaining unknown tool names. All 13 harness tests passed. The [redacted trace](observations/tool-bare-catalog-loopback-2026-09-14.json) shows Read, Bash, and Edit present in the main request and Glob/Grep absent; the synthetic Read result completed with exit 0. This is a `--bare` observation; normal startup is compared below.

A macOS `sandbox-exec` profile was tested before normal startup: a sandboxed child reached a disposable localhost server, while a direct connection to `1.1.1.1:443` failed and external DNS resolution failed. The same profile then ran the installed CLI without `--bare`, with only the synthetic local Messages endpoint reachable. All 13 harness tests passed. The [redacted normal-startup trace](observations/tool-default-catalog-loopback-2026-09-14.json) shows Read/Bash/Edit present and Glob/Grep absent in the main request, synthetic Read completion, exit 0, and empty stderr. The trace retains only fixed tool-name booleans. Tool behavior and real-service outcomes remain open.

Using the same localhost-only sandbox, the [explicit Glob trace](observations/tool-glob-enabled-loopback-2026-09-14.json) shows a main request with Glob present and Read/Bash/Edit/Grep absent among the fixed tracked names. It ends in synthetic text completion, exit 0, and empty stderr. All 13 harness tests passed. This establishes catalog selection only; Glob calls, pattern matching, permissions, and real-service outcomes remain open.

The [explicit Grep trace](observations/tool-grep-enabled-loopback-2026-09-14.json) likewise shows Grep present and Read/Bash/Edit/Glob absent among the fixed tracked names under the localhost-only sandbox. It ends in synthetic completion, exit 0, and empty stderr; all 13 harness tests passed. Query execution, result shape, permissions, and real-service behavior remain open.

The [synthetic Glob call trace](observations/tool-glob-normal-loopback-2026-09-14.json) records a `pattern: *.txt` call under the same sandbox. All 14 harness tests passed. The installed CLI returned one non-error tool result containing the synthetic fixture name, left the file unchanged, then accepted a synthetic completion and exited 0 with empty stderr. The harness retained no raw result text, paths, or request body. Exact formatting, no-match/invalid patterns, permissions, nox comparison, and real-service behavior remain open.

The [synthetic Grep call trace](observations/tool-grep-normal-loopback-2026-09-14.json) records a `pattern: alpha` call in the same fixture and sandbox. All 14 harness tests passed. The installed CLI returned one non-error tool result containing the synthetic fixture name, left the file unchanged, then accepted synthetic completion and exited 0 with empty stderr. The harness retained no raw result text, paths, or request body. Exact formatting, no-match/invalid queries, permissions, nox comparison, and real-service behavior remain open.

The [Glob no-match trace](observations/tool-glob-no-match-loopback-2026-09-14.json) uses `pattern: absent-*.zzz` in the same sandbox. All 14 harness tests passed. The installed CLI returned one non-error tool result without the synthetic fixture name, left the file unchanged, then exited 0 after synthetic completion. The result text remains redacted, so exact empty-result wording and invalid-pattern behavior are still open.

The [Grep no-match trace](observations/tool-grep-no-match-loopback-2026-09-14.json) uses `pattern: absent-sentinel` in the same sandbox. All 14 harness tests passed. The installed CLI returned one non-error tool result without the synthetic fixture name, left the file unchanged, then exited 0 after synthetic completion. The result text remains redacted, so exact empty-result wording and invalid-query behavior are still open.

The [Glob bracket trace](observations/tool-glob-bracket-loopback-2026-09-14.json) uses `pattern: [` in the same sandbox. All 14 harness tests passed. The installed CLI returned a tool result with `is_error: true`, no fixture name, unchanged file, and synthetic completion with exit 0. The diagnostic text remains redacted; this establishes the error decision only.

The [Grep bracket trace](observations/tool-grep-bracket-loopback-2026-09-14.json) repeats that input under explicit Grep selection. All 14 harness tests passed. The installed CLI returned a tool result with `is_error: true`, no fixture name, unchanged file, and synthetic completion with exit 0. Its diagnostic text remains redacted, so only the error decision is established.

## Explicit Glob/Grep offline slice (2026-09-14)

Six fixed-format synthetic traces classify the installed CLI's result text without retaining arbitrary output: Glob returns `fixture.txt` for `*.txt`, Grep returns `Found 1 file\nfixture.txt` for `alpha`, and both return `No files found` for the respective miss. An invalid `[` rejects for both, with diagnostic text still redacted. Twelve harness tests passed. Nox's new explicit `Glob`/`Grep` definitions adapt its existing `find`/`grep` engines to those observed result shapes. The seven new tool tests first failed because the definitions were absent, then passed; the neighboring tool suite passed 110 tests across six files, and `npm run check` passed. Existing lower-case tools and the default active catalog remain unchanged. This verifies the six local tool-definition decisions, not a full nox provider dispatch or real-service parity; paths, flags, limits, permissions, and exact error diagnostics remain open.

An in-memory SDK session also confirms that `tools: ["Glob"]` and `tools: ["Grep"]` each place exactly that name in `agent.state.tools`, while the default agent catalog remains `read,bash,edit,write`. The focused default-tool suite passed eight tests without a provider call. The serialized Messages request itself remains unverified in nox.

The localhost-only schema probe records fixed property presence for the installed reference tools. Glob advertises only `pattern` and `path`; Grep advertises `pattern`, `path`, `glob`, `type`, `output_mode`, `head_limit`, `offset`, `-i`, `-n`, `-o`, `-A`, `-B`, `-C`, `context`, and `multiline` in this build. The redacted [Glob](observations/tool-glob-schema-loopback-2026-09-14.json) and [Grep](observations/tool-grep-schema-loopback-2026-09-14.json) traces contain no descriptions or arbitrary input data. The nox adapters currently inherit `find`/`grep` schemas, so their advertised input fields differ even though the six minimal tool calls pass. Schema and option semantics remain an open TOOL-002/003 discrepancy, with offline option probes needed before those arguments are implemented.

Three more localhost-only calls exercised optional search inputs in disposable files: [Glob `path: nested`](observations/tool-glob-path-loopback-2026-09-14.json) returned exactly `nested/fixture.txt`; [Grep `output_mode: files_with_matches`](observations/tool-grep-files-mode-loopback-2026-09-14.json) returned `Found 1 file\nfixture.txt`; [Grep `output_mode: content`](observations/tool-grep-content-mode-loopback-2026-09-14.json) returned `fixture.txt:1:alpha`. All were non-error results with unchanged fixtures and synthetic completion. Fourteen harness tests passed. Other Grep options remain unobserved; the nox adapter currently lacks the reference `output_mode` field.

Nox's explicit Glob schema now advertises only `pattern` and `path`; the adapter prefixes nested results with the requested relative path. Explicit Grep now advertises `output_mode: files_with_matches|content` and formats `content` results as `file:line:text`, preserving the existing lower-case grep behavior. Three option tests failed before the change and pass afterward. The six-file neighboring tool suite passed 116 tests, `npm run check` passed, and no provider call was made. Reference Grep's other advertised fields (`type`, offsets, line/context flags, and multiline) remain unimplemented and need option-specific offline observations.

The [case-insensitive Grep trace](observations/tool-grep-ignore-case-loopback-2026-09-14.json) uses `pattern: ALPHA` and `-i: true` against lowercase `alpha`. The localhost-only reference tool returns the exact allowlisted one-file format, no error, and synthetic completion; the fixture remains unchanged. Fourteen harness tests passed. The default case-sensitive decision and other Grep flags remain separate work.

Nox's explicit Grep schema now accepts `-i: true` and maps it to the existing grep engine's case-insensitive option. The focused test failed before that field was added and then passed with the exact one-file synthetic output. The neighboring six-file suite passed 117 tests and `npm run check` passed. Lower-case grep and the default catalog were not changed.

The reference Grep schema advertises `output_mode: count` alongside `content` and `files_with_matches`. The [count-mode trace](observations/tool-grep-count-mode-loopback-2026-09-14.json) classifies its one-match result exactly as `fixture.txt:1\n\nFound 1 total occurrence across 1 file.` with no error and unchanged fixture. Fourteen harness tests passed. Multi-file and multi-match count grammar is not yet observed.

The [two-file count trace](observations/tool-grep-count-multiple-loopback-2026-09-14.json) uses two `alpha` lines in `fixture.txt` and one in `second.txt`. The allowlisted result is `second.txt:1\nfixture.txt:2\n\nFound 3 total occurrences across 2 files.` Both files remained unchanged. The observed order is a fixture observation, not yet a universal ordering rule. Fourteen harness tests passed.

Nox's explicit Grep count mode now reports per-file counts and total occurrences for the observed one- and two-file fixtures. Two tests failed before implementation and pass afterward; the neighboring six-file tool suite passed 119 tests and `npm run check` passed. The multi-file test checks the per-file counts independent of order because no stable reference ordering rule has been established. The adapter counts the existing grep engine's returned match lines, whose default result cap is 100; larger result sets and multiple matches on one line remain unverified and must not be claimed as parity.

The [count no-match trace](observations/tool-grep-count-no-match-loopback-2026-09-14.json) returns `No matches found\n\nFound 0 total occurrences across 0 files.` with `is_error: false`, unchanged fixture, and synthetic completion. Fourteen harness tests passed. Nox currently returns `No files found` for this input, so count-mode empty-result formatting remains open for T147.

Nox's explicit Grep count-mode no-match branch now returns the observed zero-total text while the other Grep no-match modes keep `No files found`. The new test failed before the change and passed afterward. The neighboring six-file suite passed 120 tests, and `npm run check` passed.

The [101-line count trace](observations/tool-grep-count-limit-loopback-2026-09-14.json) returns `fixture.txt:101\n\nFound 101 total occurrences across 1 file.` under the localhost-only reference fixture, with no error and unchanged file. Fourteen harness tests passed. This exceeds the lower-case nox grep tool's default 100-match cap, so the explicit count adapter needs a separate exact-count path before this case can match.

The 101-line nox count test first failed at 100 because the explicit adapter counted the lower-case grep tool's truncated result. Count mode then called local ripgrep through a separate bounded, no-shell execution path; the lower-case Pi grep tool remained unchanged. The 0-, 1-, 3-, and 101-line fixtures passed. The neighboring six-file suite passed 121 tests and `npm run check` passed. The helper caps command output at 8 MiB and rejects execution errors rather than reporting a partial total; behavior beyond that cap and reference ordering remain open.

The [same-line count trace](observations/tool-grep-count-same-line-loopback-2026-09-14.json) has two `alpha` occurrences on one line. The reference reports `fixture.txt:1\n\nFound 1 total occurrence across 1 file.`—its count mode counts matching lines despite the word “occurrence.” Nox's first dedicated count command reported 2, so the explicit helper now uses ripgrep `--count` rather than `--count-matches`. The new test failed before that correction, then the six-file suite passed 122 tests and `npm run check` passed. Fourteen offline harness tests passed; no account call was made.

The [Grep no-line-number trace](observations/tool-grep-no-line-number-loopback-2026-09-14.json) uses `output_mode: content` and `-n: false`. It returns exactly `fixture.txt:alpha`, no error, unchanged fixture, and synthetic completion. Fourteen harness tests passed. Nox's explicit Grep schema does not yet advertise `-n` and its content mode includes `:1:`, so this is an open result and schema gap for T152.

Nox's explicit Grep schema now accepts `-n: false` and omits `:line:` in content-mode results, matching the synthetic `fixture.txt:alpha` fixture. The new test failed before the change and passed afterward. The neighboring six-file suite passed 123 tests, and `npm run check` passed. The lower-case Pi grep tool remains unchanged.

The [Grep only-matching trace](observations/tool-grep-only-matching-loopback-2026-09-14.json) uses `output_mode: content`, `-o: true`, and a synthetic line `alpha beta`. It returns exactly `fixture.txt:1:alpha`, omitting ` beta`; the result is non-error and the file is unchanged. Fourteen harness tests passed. Nox's explicit Grep schema has no `-o` field yet, so this is open for T154.

Nox's explicit Grep schema now accepts `-o: true`. For observed content-mode calls it runs ripgrep's match-only output through a bounded, no-shell path, returning `fixture.txt:1:alpha` for the `alpha beta` fixture. The new test failed before implementation and passed afterward. The neighboring six-file suite passed 124 tests and `npm run check` passed. Other combinations of `-o` with output modes or flags remain unobserved; lower-case Pi grep is unchanged.

The [Grep context trace](observations/tool-grep-context-loopback-2026-09-14.json) uses `output_mode: content` and `-C: 1` against `alpha\nbeta\n`. It returns exactly `fixture.txt:1:alpha\nfixture.txt-2-beta`: the match uses colons and the following context line uses dashes. The result is non-error, the fixture is unchanged, and fourteen harness tests passed. Nox does not yet advertise `-C` in its explicit Grep schema; T156 remains open.

Nox's explicit Grep schema now accepts nonnegative `-C`; the adapter maps it to the existing grep engine's context input and formats match/context separators as observed. The new test failed before implementation and passed afterward. The neighboring six-file suite passed 125 tests, and `npm run check` passed. Lower-case Pi grep remains unchanged; `-A` and `-B` are still unobserved and unsupported by the adapter.

The [after-context](observations/tool-grep-after-context-loopback-2026-09-14.json) and [before-context](observations/tool-grep-before-context-loopback-2026-09-14.json) traces use `before\nalpha\nafter\n`. With `-A: 1`, the reference returns `fixture.txt:2:alpha\nfixture.txt-3-after`; with `-B: 1`, it returns `fixture.txt-1-before\nfixture.txt:2:alpha`. Both are non-error, leave the file unchanged, and complete synthetically. Fourteen harness tests passed; nox's explicit adapter does not yet advertise either field.

Nox's explicit Grep schema now accepts nonnegative `-A` and `-B` values. Those content-mode calls use local ripgrep's one-sided context flags through a bounded, no-shell helper, preserving the observed match/context separators and order. Both new tests failed before the change and passed afterward. The neighboring six-file suite passed 127 tests, and `npm run check` passed. Interactions among `-A`, `-B`, `-C`, `-n`, and `-o` remain unobserved; lower-case Pi grep is unchanged.

The [nested Grep trace](observations/tool-grep-path-loopback-2026-09-14.json) uses `path: nested` without `output_mode`. It returns `nested/fixture.txt:1:alpha`, unlike the no-path default file-list result. Both root and nested fixtures remain unchanged, the result is non-error, and fourteen harness tests passed. This is a fixture-specific implicit-mode observation; T160 will check explicit `files_with_matches` precedence before nox source work.

The [nested explicit-files trace](observations/tool-grep-path-files-loopback-2026-09-14.json) adds `output_mode: files_with_matches` to the same `path: nested` call and returns `Found 1 file\nnested/fixture.txt`. The explicit mode takes precedence over the implicit nested-path content shape. Both fixtures remain unchanged, the result is non-error, and fourteen harness tests passed. T161 can now implement both observed branches without guessing precedence.

Nox's explicit Grep adapter now prefixes results from `path: nested` with the repository-relative search directory. With no `output_mode`, it emits the observed content result; with explicit `files_with_matches`, it emits the observed file list. Both new tests failed before the change and passed afterward. The neighboring six-file suite passed 129 tests, and `npm run check` passed. Lower-case Pi grep and no-path defaults remain unchanged.

The offline loopback harness now derives sandboxing, catalog capture, unrestricted-tool selection, and expected fixture-match decisions from one declared search-mode set. This removes repeated mode lists that had required synchronized edits. Fourteen harness tests and five representative sandboxed traces (bare catalog, Glob path, Grep nested files, Grep no-match count, and invalid Grep) passed after the refactor. No reference account call or source-tool behavior changed.

The [Grep glob-filter trace](observations/tool-grep-glob-filter-loopback-2026-09-14.json) sends `glob: *.txt` with matching synthetic `fixture.txt` and `fixture.md`. The reference returns exactly `Found 1 file\nfixture.txt`; both files remain unchanged and the result is non-error. Fourteen harness tests passed. Nox's explicit Grep adapter already matches this behavior; its new focused test passed with the existing implementation (24 tests in that file). The probe used only the localhost synthetic endpoint under the macOS network sandbox; no account call was made.

The [Grep head-limit trace](observations/tool-grep-head-limit-loopback-2026-09-14.json) uses `output_mode: content`, `head_limit: 1`, and two matching lines in one synthetic file. The reference returns exactly `fixture.txt:1:alpha\n\n[Showing results with pagination = limit: 1]`; the file is unchanged and the result is non-error. Fourteen harness tests passed. Nox's explicit Grep schema lacks `head_limit`, so T165 will implement this observed branch. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit Grep schema now accepts a positive `head_limit` and its content-mode adapter limits the observed two-line result to one line with the reference pagination marker. The focused test failed before implementation; the neighboring six-file suite then passed 137 tests and `npm run check` passed. Other `head_limit` modes and pagination boundaries remain unobserved.

The [Grep offset trace](observations/tool-grep-offset-loopback-2026-09-14.json) uses `output_mode: content`, `head_limit: 1`, and `offset: 1` against two matching lines. The reference returns exactly `fixture.txt:2:alpha\n\n[Showing results with pagination = offset: 1]`; the fixture is unchanged and the result is non-error. Fourteen harness tests passed. Nox's explicit Grep schema lacks `offset`, so T167 covers this observed branch. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit Grep schema now accepts nonnegative `offset`. For the observed content-mode call it selects the second result and emits the reference offset pagination marker. The new test failed before implementation; the neighboring six-file suite passed 138 tests and `npm run check` passed. Other offset boundaries and modes remain unobserved.

The [Grep exact-limit trace](observations/tool-grep-head-exact-loopback-2026-09-14.json) uses `output_mode: content` and `head_limit: 1` with exactly one matching line. The reference returns only `fixture.txt:1:alpha`, without a pagination marker; the fixture is unchanged and the result is non-error. Nox's existing adapter matches this boundary (27 focused tests passed). Fourteen harness tests passed, and the reference probe remained on the localhost synthetic endpoint under the macOS network sandbox.

The [Grep offset-end trace](observations/tool-grep-offset-end-loopback-2026-09-14.json) uses `output_mode: content`, `head_limit: 1`, and `offset: 2` with only two matching lines. The reference returns exactly `No entries at this offset\n\n[Showing results with pagination = offset: 2]`, with no error and an unchanged fixture. Fourteen harness tests passed. Nox currently emits an empty result body with a marker, so T170 will close this boundary gap. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's content-mode pagination now emits `No entries at this offset` when the selected result page is empty. The new test failed with only the marker before the change; the neighboring six-file suite passed 140 tests and `npm run check` passed afterward. Other pagination modes remain unobserved.

The [Grep zero-offset trace](observations/tool-grep-offset-zero-loopback-2026-09-14.json) uses `output_mode: content`, `head_limit: 1`, and explicit `offset: 0` against two matching lines. The reference returns `fixture.txt:1:alpha\n\n[Showing results with pagination = limit: 1]`, the same marker as an omitted offset. Nox already matches this case (29 focused tests passed); fourteen harness tests and `npm run check` passed. The fixture remained unchanged, and the probe used only the localhost synthetic endpoint under the macOS network sandbox.

The [Grep type-filter trace](observations/tool-grep-type-filter-loopback-2026-09-14.json) sends `type: py` with matching synthetic `fixture.py` and `fixture.txt`. The reference returns exactly `Found 1 file\nfixture.py`; both files are unchanged and the result is non-error. Fourteen harness tests passed. Nox's explicit Grep schema has no `type` field, so T173 will implement this observed filter. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit Grep schema now accepts a nonempty `type` string and passes it as a separate ripgrep argument, so the observed `py` filter selects only the Python fixture. The lower-case Pi grep schema and default behavior remain unchanged; its internal executor accepts an optional file type only when the explicit adapter supplies one. The new test failed before implementation; the neighboring six-file suite passed 142 tests and `npm run check` passed. Other types and type/flag combinations remain unobserved.

The [Grep context-alias trace](observations/tool-grep-context-alias-loopback-2026-09-14.json) sends `output_mode: content` and `context: 1` against `alpha\nbeta`. The reference returns exactly `fixture.txt:1:alpha\nfixture.txt-2-beta`, matching the earlier `-C: 1` trace; the result is non-error and the fixture is unchanged. Fourteen harness tests passed. Nox's explicit Grep schema lacks `context`, so T175 will map this observed alias. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit Grep schema now accepts nonnegative `context` and maps it through the existing symmetric context path when `-C` is absent. The new test failed before implementation; the neighboring six-file suite passed 143 tests and `npm run check` passed afterward. Precedence when both fields are supplied remains unobserved.

The [Grep multiline trace](observations/tool-grep-multiline-loopback-2026-09-14.json) sends `output_mode: content`, `multiline: true`, and a two-line `alpha\nbeta` pattern against the synthetic fixture. The reference returns `fixture.txt:1:alpha\nfixture.txt:2:beta`; the result is non-error and the fixture is unchanged. Fourteen harness tests passed. Nox's explicit Grep schema has no `multiline` field, and its lower-case grep engine does not enable multiline matching, so T177 needs a dedicated bounded path. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit Grep schema now accepts `multiline`. The observed content-mode branch runs ripgrep with `--multiline` through a bounded, no-shell helper and returns both numbered lines. The new test failed before implementation; the neighboring six-file suite passed 144 tests and `npm run check` passed afterward. Other multiline output modes and flag combinations remain unobserved; the lower-case Pi grep path is unchanged.

The [multiline no-match trace](observations/tool-grep-multiline-no-match-loopback-2026-09-14.json) sends `output_mode: content`, `multiline: true`, and an absent synthetic pattern. The reference returns exactly `No matches found`, without error and with the fixture unchanged. Fourteen harness tests passed. Nox's new multiline helper currently returns `No files found` for this case, so T179 will correct the empty-result text. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's multiline helper now emits `No matches found` when ripgrep returns no matches. The new focused test failed before that change; the neighboring six-file suite passed 145 tests and `npm run check` passed afterward. The non-multiline empty-result behavior remains unchanged.

The [multiline default-mode trace](observations/tool-grep-multiline-files-loopback-2026-09-14.json) sends `multiline: true` and `alpha\nbeta` without `output_mode`. The reference returns exactly `Found 1 file\nfixture.txt`; the result is non-error and the fixture is unchanged. Fourteen harness tests passed. Nox's explicit adapter currently routes only content-mode multiline calls through its helper, so T181 will handle this observed default file-list branch. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit adapter now routes default-mode multiline calls through the bounded helper and formats its parsed matches as a unique file list. The new test failed with ripgrep's newline-pattern error before the change; the neighboring six-file suite passed 146 tests and `npm run check` passed afterward. Explicit `files_with_matches` with multiline remains unobserved.

The [multiline explicit-files trace](observations/tool-grep-multiline-explicit-files-loopback-2026-09-14.json) sends `multiline: true`, `output_mode: files_with_matches`, and `alpha\nbeta`. The reference returns `Found 1 file\nfixture.txt`, matching the default-mode file list. Fourteen harness tests passed; the fixture remained unchanged and the result was non-error. Nox does not yet route this explicit branch through its multiline helper, so T183 will add it. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox now routes explicit `files_with_matches` multiline calls through the bounded helper's file-list formatter. The new test failed with ripgrep's newline-pattern error before the change; the neighboring six-file suite passed 147 tests and `npm run check` passed afterward. Multiline count mode remains unobserved.

The [multiline count trace](observations/tool-grep-multiline-count-loopback-2026-09-14.json) sends `multiline: true`, `output_mode: count`, and a two-line `alpha\nbeta` pattern. The reference returns `fixture.txt:1\n\nFound 1 total occurrence across 1 file.`, counting the spanning match once. Fourteen harness tests passed; the result is non-error and the fixture unchanged. Nox's count helper lacks the ripgrep multiline flag, so T185 will add it. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit count helper now passes ripgrep `--multiline` when requested. The new test failed with ripgrep's newline-pattern error before the change; the neighboring six-file suite passed 148 tests and `npm run check` passed afterward. Other multiline count boundaries remain unobserved.

The US1 search inventory, scenario table, reconciliation, and gated register now point to the synthetic normal/no-match/invalid Glob and Grep traces and the actual nox search adapter/source files. T055 and T061 are closed for the isolated localhost observations. Exact invalid diagnostics, interactive permission decisions, and real-service behavior remain gated; no account call was made. All newly referenced observation links resolve.

The [two-file Glob trace](observations/tool-glob-two-files-loopback-2026-09-14.json) uses `pattern: *.txt` with synthetic `fixture.txt` and `second.txt`. The reference returns exactly `fixture.txt\nsecond.txt`; both files remain unchanged and the result is non-error. Nox's explicit Glob adapter already matches this order (37 focused tests passed). Fourteen harness tests passed; the reference probe used only the localhost synthetic endpoint under the macOS network sandbox. Ordering for other file names and nested paths remains unobserved.

The [recursive Glob trace](observations/tool-glob-recursive-loopback-2026-09-14.json) uses `pattern: **/*.txt` with root and nested `fixture.txt` files. The reference returns exactly `fixture.txt\nnested/fixture.txt`; both files remain unchanged and the result is non-error. Nox's explicit adapter already matches this path shape and order (38 focused tests passed). Fourteen harness tests passed; the probe used only the localhost synthetic endpoint under the macOS network sandbox.

The [hidden-file Glob trace](observations/tool-glob-hidden-loopback-2026-09-14.json) uses `pattern: *.txt` with `fixture.txt` created before `.hidden.txt`. The reference returns `fixture.txt\n.hidden.txt`, with both files unchanged and no tool error. The new nox test fails because its fd-backed adapter returns `.hidden.txt\nfixture.txt`. Fourteen harness tests passed. T190 will control file modification times to identify the ordering rule before changing source. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

The [controlled-mtime Glob trace](observations/tool-glob-mtime-loopback-2026-09-14.json) gives `.hidden.txt` an older modification time than `fixture.txt`, reversing their creation order. The reference returns `.hidden.txt\nfixture.txt`; contents and timestamps remain unchanged. Together with the earlier trace, this supports ascending modification-time ordering for these two files rather than fd's lexical order. Fourteen harness tests passed, and the probe used only the localhost synthetic endpoint under the macOS network sandbox. T191 will add the explicit-adapter sort.

Nox's explicit Glob adapter now sorts matched paths by ascending modification time, with the original find order as a tie break. Deterministic tests cover both visible-first and hidden-first timestamp orders; the visible-first test failed before implementation. The adapter preserves the lower-case find result-limit notice, verified with a 1000-result fixture. The neighboring six-file suite passed 153 tests, but the initial `npm run check` failed because this TypeScript target lacks `toSorted` and the notice test accessed a union-typed content block. Those compatibility errors were corrected in the following task; the lower-case Pi find implementation is unchanged.

The [equal-mtime Glob trace](observations/tool-glob-mtime-tie-loopback-2026-09-14.json) sets both synthetic files to the same modification time. The reference returns `.hidden.txt\nfixture.txt`; contents and timestamps remain unchanged. Nox's explicit adapter preserves fd order for equal timestamps and matches this fixture. Fourteen harness tests passed, and the probe used only the localhost synthetic endpoint under the macOS network sandbox. After replacing `toSorted` with a sorted copy and fixing the test assertion, the neighboring six-file suite passed 154 tests and `npm run check` passed.

The [ignored-file Glob trace](observations/tool-glob-ignored-loopback-2026-09-14.json) adds `.gitignore` with `ignored.txt` and a matching synthetic `ignored.txt` beside `fixture.txt`. The reference returns both as `fixture.txt\nignored.txt`; files remain unchanged and the result is non-error. The new nox test fails because its fd-backed find excludes the ignored file. Fourteen harness tests passed. T194 will fix only the explicit Glob adapter while preserving lower-case Pi find's gitignore behavior. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit Glob adapter now asks its fd-backed find executor to include files excluded by `.gitignore`; lower-case Pi find retains its existing git-aware default. The new test failed before implementation; the neighboring six-file suite passed 155 tests and `npm run check` passed afterward. Other ignore-file sources and patterns remain unobserved.

The [.ignore Glob trace](observations/tool-glob-fd-ignore-loopback-2026-09-14.json) adds `.ignore` with `ignored.txt` and a matching synthetic file. The reference still returns `fixture.txt\nignored.txt`; the files remain unchanged and the result is non-error. Fourteen harness tests passed. The new nox test fails because fd's `--no-ignore-vcs` still honors `.ignore`. T196 will include this file in the explicit adapter. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

The [Glob git-metadata trace](observations/tool-glob-git-metadata-loopback-2026-09-14.json) adds synthetic `.git/inner.txt` to the disposable repository and searches `**/*.txt`. The reference returns `fixture.txt\n.git/inner.txt`, with both files unchanged and no tool error. Fourteen harness tests passed. This supports using fd's broader no-ignore mode for the explicit adapter in T196; the probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit Glob adapter now uses fd's `--no-ignore`, matching the observed `.gitignore`, `.ignore`, and `.git` fixtures. The `.ignore` test failed before the change; the neighboring six-file suite passed 157 tests and `npm run check` passed afterward. Lower-case Pi find retains its default ignore rules. Other exclusion mechanisms remain unobserved.

The [ignored-file Grep trace](observations/tool-grep-ignored-loopback-2026-09-14.json) uses the same `.gitignore`-excluded matching file as the Glob probe, but the reference Grep returns only `Found 1 file\nfixture.txt`. Grep therefore honors this gitignore rule in the synthetic fixture while Glob does not. Nox's explicit Grep adapter already matches the result (46 focused tests passed); fourteen harness tests passed. The files remained unchanged and the probe used only the localhost synthetic endpoint under the macOS network sandbox.

The [.ignore Grep trace](observations/tool-grep-fd-ignore-loopback-2026-09-14.json) excludes a synthetic matching `ignored.txt` through `.ignore`. The reference again returns only `Found 1 file\nfixture.txt`; files are unchanged and the result is non-error. Nox's explicit Grep adapter already matches (47 focused tests passed), and fourteen harness tests passed. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

The [Grep git-metadata trace](observations/tool-grep-git-metadata-loopback-2026-09-14.json) adds synthetic `.git/inner.txt` inside a real disposable repository. The reference Grep returns only `Found 1 file\nfixture.txt`, excluding metadata unlike Glob. Nox's current grep includes the metadata file even after the nox test fixture was changed to a real initialized repository. Fourteen harness tests passed; files remained unchanged and the probe used only the localhost synthetic endpoint under the macOS network sandbox. T201 will distinguish generic hidden-file behavior before a source fix.

The [hidden-file Grep trace](observations/tool-grep-hidden-loopback-2026-09-14.json) adds synthetic `.hidden.txt`. The reference returns `Found 2 files\n.hidden.txt\nfixture.txt`, including the hidden file before the visible file; both files remain unchanged and the result is non-error. Nox includes both but in the opposite order. Fourteen harness tests passed, and the probe used only the localhost synthetic endpoint under the macOS network sandbox. T202 will control modification times before changing the adapter's ordering.

The [controlled-mtime Grep trace](observations/tool-grep-mtime-loopback-2026-09-14.json) makes `.hidden.txt` older than `fixture.txt`. The reference reverses the earlier order to `Found 2 files\nfixture.txt\n.hidden.txt`; contents and timestamps remain unchanged. Together the traces support descending modification-time order for Grep file lists, opposite Glob's ascending order in these fixtures. Fourteen harness tests passed, and the probe used only the localhost synthetic endpoint under the macOS network sandbox. T203 will also close the observed `.git` metadata gap.

Nox's explicit Grep file-list adapter now orders files by descending modification time and excludes `.git` metadata while retaining regular hidden matches. The hidden-newer and metadata tests failed before implementation; the neighboring six-file suite passed 164 tests and `npm run check` passed afterward. The match-limit notice is preserved after sorting. Dedicated explicit Grep helpers use the same `.git` exclusion as a consistency inference; their metadata behavior has not yet been observed against the reference. Lower-case Pi grep is unchanged.

The [equal-mtime Grep trace](observations/tool-grep-mtime-tie-loopback-2026-09-14.json) gives `.hidden.txt` and `fixture.txt` the same controlled modification time. The reference returns `Found 2 files\n.hidden.txt\nfixture.txt`, with contents and timestamps unchanged. Nox's current tie-break keeps ripgrep traversal order and returns the reverse; the focused test failed. Fourteen harness tests passed, and the probe used only the localhost synthetic endpoint under the macOS network sandbox. T205 will add a deterministic tie-break.

Nox's explicit Grep file-list adapter now uses path order when modification times tie. The nox tie test failed once and passed once before the change, exposing nondeterministic ripgrep traversal; after the change the neighboring six-file suite passed 165 tests, the focused file passed again, and `npm run check` passed. The tie rule is confirmed only for the hidden/visible fixture; other equal-time names remain unobserved.

The [two-file Grep content trace](observations/tool-grep-content-multiple-loopback-2026-09-14.json) sets `fixture.txt` older than `second.txt` and searches `alpha` in `output_mode: content`. The reference returns `second.txt:1:alpha\nfixture.txt:1:alpha`, with both files and timestamps unchanged and no tool error. This matches the descending file-time rule observed for file-list mode. Fourteen harness tests passed, and the probe used only the localhost synthetic endpoint under the macOS network sandbox. T207 will compare and implement this content ordering in nox.

Nox's explicit Grep content branch now orders matching lines by descending file modification time, with a path tie-break and original line order within each file. The two-case nox test failed for one timestamp order before the change; the neighboring six-file suite passed 167 tests and `npm run check` passed afterward. The lower-case Pi grep path remains unchanged.

The [equal-time Grep content trace](observations/tool-grep-content-tie-loopback-2026-09-14.json) gives `fixture.txt` and `second.txt` the same controlled modification time. The reference returns `second.txt:1:alpha\nfixture.txt:1:alpha`, whereas nox's path tie-break would put `fixture.txt` first. Both files and timestamps are unchanged, the result is non-error, and fourteen harness tests passed. T209 will reverse creation order to distinguish a creation-time tie-break from a fixed path rule; the probe used only the localhost synthetic endpoint under the macOS network sandbox.

The [reversed-creation Grep content trace](observations/tool-grep-content-tie-reversed-loopback-2026-09-14.json) recreates `fixture.txt` after `second.txt` but gives both equal modification times. The reference still returns `second.txt:1:alpha\nfixture.txt:1:alpha`; both files and timestamps remain unchanged and the result is non-error. This rules out creation order for the observed tie and supports descending path order in content mode, unlike file-list mode's hidden/visible tie. Fourteen harness tests passed; the probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit Grep content branch now breaks equal-time ties by descending path, while keeping original line order within a file. The equal-time test failed before implementation; the neighboring six-file suite passed 168 tests and `npm run check` passed afterward. Other equal-time path pairs remain unobserved.

The [two-file content pagination trace](observations/tool-grep-content-page-loopback-2026-09-14.json) uses `head_limit: 1` with `second.txt` newer than `fixture.txt`. The reference returns only `second.txt:1:alpha` followed by the `limit: 1` pagination marker, showing that this page selects the newer file first. Both files and timestamps remain unchanged, and the result is non-error. Nox already matches the observed result (57 focused tests passed); fourteen harness tests passed. The probe used only the localhost synthetic endpoint under the macOS network sandbox.

The [file-list pagination trace](observations/tool-grep-files-page-loopback-2026-09-14.json) uses explicit `files_with_matches` and `head_limit: 1` across two files with `second.txt` newer. The reference returns exactly `Found 1 file limit: 1\nsecond.txt`; both files and timestamps remain unchanged and the result is non-error. Nox currently ignores the file-list head limit, so T213 will implement this observed branch. Fourteen harness tests passed; the probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit Grep file-list adapter now selects the first `head_limit` files after sorting and annotates the header when more matches exist. The new test failed with both files before the change; the neighboring six-file suite passed 170 tests and `npm run check` passed afterward. File-list offset and exact-limit boundaries remain unobserved.

The [file-list exact-limit trace](observations/tool-grep-files-exact-loopback-2026-09-14.json) uses explicit `files_with_matches` and `head_limit: 1` with exactly one matching file. The reference returns `Found 1 file\nfixture.txt`, without a limit annotation. Nox already matches this boundary (59 focused tests passed); fourteen harness tests passed. The fixture remained unchanged, the result was non-error, and the probe used only the localhost synthetic endpoint under the macOS network sandbox.

The [file-list offset trace](observations/tool-grep-files-offset-loopback-2026-09-14.json) uses `files_with_matches`, `head_limit: 1`, and `offset: 1` across two files with `second.txt` newer. The reference returns `Found 1 file offset: 1\nfixture.txt`, selecting the older second entry. Both files and timestamps remain unchanged and the result is non-error. The nox test fails because it ignores offset and returns the first file with a limit annotation. Fourteen harness tests passed; the probe used only the localhost synthetic endpoint under the macOS network sandbox.

Nox's explicit Grep file-list adapter now applies offset after descending-time sorting and uses the reference's offset header. The new test failed before implementation; the neighboring six-file suite passed 172 tests and `npm run check` passed afterward. File-list offset-past-end behavior remains unobserved.

The [file-list offset-end trace](observations/tool-grep-files-offset-end-loopback-2026-09-14.json) uses `files_with_matches`, `head_limit: 1`, and `offset: 2` with two matching files. The reference returns the single-line text `No entries at this offset. [Showing results with pagination = offset: 2]`, without a tool error; files and timestamps remain unchanged. This differs from content mode's two-line empty-page result. Fourteen harness tests passed, and the probe used only the localhost synthetic endpoint under the macOS network sandbox. T218 will match this boundary in nox.

Nox's explicit Grep file-list adapter now emits the reference's single-line message when offset passes the last matching file. The new test failed with an empty `Found 0 files` body before the change; the neighboring six-file suite passed 173 tests and `npm run check` passed afterward. Other offset boundaries remain unobserved.

The installed top-level help has eight short aliases. Four (`-c`, `-h`, `-p`, and `-r`) already appeared in CLI inventory inputs; T219 added the missing `-d`, `-n`, `-v`, and `-w` as CLI-563–CLI-566. An offline extraction confirmed all 65 installed long flags and all eight short aliases now appear in the top-level inventory or additional-alias leaves. Alias invocation behavior remains unobserved, and T002 stays open.

T220 compared option lines and short aliases in the captured nested `help-*.txt` files with inventory rows linked to each capture. All ordinary nested options have a corresponding row. The two daemon capture exceptions are repeated parent help, already represented by CLI-552; no new leaf was warranted. Invocation behavior and undocumented combinations remain open.

T221 extracted 42 entries from captured nested `Commands:` blocks and matched each command path to an inventory leaf. The `plugin|plugins` usage alias was normalized to the existing plugin family; no printed nested command was missing. This is a help-surface audit, not invocation proof.

T222 ran installed `claude -v` and `claude --version` in a disposable home and cwd under `sandbox-exec` with all network access denied. Both returned exit 0, exactly `2.1.270 (Claude Code)\n` on stdout, empty stderr, and no filesystem entries. Nox's existing argument parser routes `-v` and `--version` to the same version branch; output branding and version numbers are product-specific. No provider call occurred.

T223 compared installed `-d` and `--debug` with a version exit and an unknown-option exit under separate disposable homes with all network denied. Both pairs matched exactly, including exit status, stdout/stderr, and no filesystem entries. This confirms parser acceptance only; debug behavior is still open. Nox's parser has no `-d`/`--debug` branch, so implementation needs a separate behavior contract.

T224 ran installed `-n` and `--name` with no value under separate disposable homes with all network denied. Both returned exit 1, empty stdout, `error: option '-n, --name <name>' argument missing\n` on stderr, and no home/cwd entries. Nox accepts both spellings but its current error text differs; T225 will implement the observed diagnostic.
